@using LeaveSystem.Web.Pages.HrPanel
@using LeaveSystem.Web.Pages.LeaveRequests.CreatingLeaveRequest
@using LeaveSystem.Web.Pages.LeaveRequests.ShowingLeaveRequests
@using LeaveSystem.Web.Pages.UserPanel

<RadzenSidebar @bind-Expanded="@SidebarExpanded">
    <RadzenPanelMenu>
        <RadzenPanelMenuItem Text="Home" Icon="home" Path="" />
        <AuthorizeView Policy="@CreateLeaveRequest.PolicyName" Context="CreateLeaveRequest">
        <RadzenPanelMenuItem Text="Create leave request" Icon="account_box" Path="create-leave-request" />
        </AuthorizeView>
        <AuthorizeView Policy="@ShowLeaveRequests.PolicyName" Context="ShowLeaveRequests">
            <RadzenPanelMenuItem Text="Leave request" Icon="account_box" Path="leave-requests" />
        </AuthorizeView>
        <AuthorizeView Policy="@ShowUserPanel.PolicyName" Context="ShowUserPanel">
            <RadzenPanelMenuItem Text="User panel" Icon="account_box" Path="user-panel" />
        </AuthorizeView>
        <AuthorizeView Policy="@ShowHrPanel.PolicyName" Context="ShowHrPanel">
            <RadzenPanelMenuItem Text="HR panel" Icon="account_box" Path="hr-panel" />
        </AuthorizeView>
    </RadzenPanelMenu>
</RadzenSidebar>

@code {
    [Parameter]
    public bool SidebarExpanded { get; set; } = true;
}

